# 附录4

## 📄 《Linux 与 LNMP 架构全栈运维实训报告》

*(注：本报告模板严格基于最新实训教学大纲定制，作为结课作业考核依据)*

## 👥 实训学生档案与成绩评定

- **班级**：________________________
- **姓名**：________________________
- **学号**：________________________
- **实训组别**：第 ______ 组（成员：________________________）
- **实训周期**：3 天（硬核全栈攻坚）
- **综合评分**：【 教师评定分区：A / B / C / D / 留级重修 】

## 一、 阶段一：阿里云 ECS 实例申请、配置与 Linux 基础

> **大纲对应考点**：选择阿里云 ECS 理由、SSH 远程连接、Linux 树状目录结构与基础操作命令。

### 1.1 阿里云 ECS 实例申请与网络拓扑规划

根据大纲要求，学生需结合“免费试用/学生认证特惠/按量付费/包年包月”等任一选型模式，开通一台 CentOS 7.9 规格的实例，并精准配置安全组规则。

- 📝 **学生实操任务 1.1**：请如实填写你所申请的云服务器底层参数：

  - **ECS 实例 ID**：________________________
  - **公网 IPv4 地址**：________________________
  - **私网 IPv4 地址**：________________________

- 🖼️ **截图提交位 1.1**：请在此粘贴你在阿里云控制台看到的该 ECS 实例**放行了 22(SSH)、80(HTTP)、443(HTTPS) 的安全组入方向规则截图**。

  > `[ 粘贴安全组入方向规则截图 ]`

### 1.2 SSH 远程连接与 Linux 目录结构验证

- 📝 **学生实操任务 1.2**：请在下方粘贴你通过终端工具执行 SSH 远程连接成功连入服务器、并敲击 `ls -l /` 勘察 Linux 根目录下各大核心目录（如 `/etc`, `/var`, `/usr`）的**回显截图或文本记录**。

  > `[ 粘贴终端连接成功并展现根目录树状结构的回显截图 ]`

### 1.3 Linux 核心命令操作与学习技巧

- 📝 **学生实操任务 1.3**：请简述你在执行 Day 01 综合实操练习中，创建测试目录、复制备份配置文件以及修改文本内容时最常用的 3 个核心命令，并写出一个你学到的命令学习小技巧。

  > **答：**

## 二、 阶段二：Web 服务工作原理与 LNMP 环境一键部署

> **大纲对应考点**：Web 工作机制理论、LNMP 相比 LAMP 的高并发优势、子配置文件路由联动支持。

### 2.1 核心理论考核（简答题）

- 📝 **学生实操任务 2.1**：当一个浏览器客户端请求访问一个动态 PHP 网页时，Nginx、PHP-FPM 与数据库三者之间是如何协同工作的？为什么说在高并发场景下 Nginx（LNMP）比 Apache（LAMP）更具性能优势？

  > **答：**

### 2.2 LNMP 工业级部署流水线

按照大纲实操步骤，依序执行：更新系统软件包 $\rightarrow$ 安装 Nginx $\rightarrow$ 安装 MariaDB 数据库 $\rightarrow$ 安装 PHP $\rightarrow$ 配置 Nginx 支持 PHP 并重载。

- 📝 **学生实操任务 2.2**：学生需在 `/etc/nginx/conf.d/default.conf` 中完成 Nginx 转发 PHP-FPM 请求的核心配置。请将你修改完成并测试重载成功的**完整子配置文件关键 location 代码块**贴在下方（必须包含 `try_files` 伪静态支持）：

  Nginx

  ```
  # [ 请在此处贴入你的 /etc/nginx/conf.d/default.conf 核心联动拦截代码 ]
  ```

## 三、 阶段三：全球第一 CMS —— WordPress 架构上线与企业级调优

> **大纲对应考点**：最小权限账号隔离、源码部署、发布第一篇博文、突破上传与内存限制、免除 FTP 凭证热更新。

### 3.1 为 WordPress 创建专属数据库与用户

严禁使用数据库 `root` 最高特权账号直连前端业务。必须遵守“最小权限原则”为 WordPress 项目隔离出专属沙箱。

- 📝 **学生实操任务 3.1**：请如实记录你为 WordPress 划分的隔离安全凭证（禁止填写 root 账号）：
  - **新建独立数据库名**：________________________
  - **新建独立本地项目用户名**：________________________

### 3.2 源码替换、Web 引导配置与首篇博文发布

规范化执行源码替换，将其覆盖到根目录 `/usr/share/nginx/html`。执行 `chown -R nginx:nginx` 彻底阻断 Linux 权限被拒引发的写入故障。

- 🖼️ **截图提交位 3.2**：请在此处粘贴你成功完成 Web 端引导配置、进入 WordPress 后台主面板并**成功发布第一篇测试博文**的前端网页全屏截图。

  > `[ 粘贴成功发布第一篇博文的网站首页或仪表盘全屏截图 ]`

### 3.3 企业级调优 —— 突破上限与免 FTP 凭证（防坑加餐）

- 📝 **学生实操任务 3.3**：请写出你是通过哪几行命令或在哪个文件的哪一行追加了代码，从而完美绕过了“要求输入 FTP 凭证”的阻碍、实现了后台一键直接热更新主题？

  > **答：**

## 四、 阶段四：分组实操安排与故障排查日志

> **大纲对应考点**：分组实操实践任务、常见错误排查与日志复盘。

### 4.1 小组协作工时与职责划分

| **小组成员姓名** | **在实训中担当的具体模块 / 角色**   | **团队协同实操心得与任务达成度** |
| ---------------- | ----------------------------------- | -------------------------------- |
| **（组长）**     | 组长 / 全链路架构设计与云资源规划   |                                  |
| **（组员 1）**   | LNMP 环境部署与 Nginx 规则配置联动  |                                  |
| **（组员 2）**   | 数据库安全加固与 WordPress 调优上线 |                                  |

### 4.2 真实排雷日志记录（核心高分项）

- 📝 **学生实操任务 4.2**：在本次从 ECS 申请到 WordPress 博客全面上线的全栈实操中，你或者你所在的团队遇到了哪一个最棘手的报错（例如：`404 Not Found`、`502 Bad Gateway`、数据库连不上、或者图片传不上去）？你们是如何通过查看系统日志排查并最终完美解决的？请详细还原你的排雷过程：

  Plaintext

  ```
  【故障现象还原】：
  
  【核心排查思路与看过的日志路径】：
  
  【最终采用的修复方案指令】：
  ```