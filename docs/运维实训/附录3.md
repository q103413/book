# 附录3

## 学生实训运维文档模板

> 适配课程：Linux 云平台综合实训 / 三天 LNMP+WordPress 部署项目
>
> 适用场景：学生实训作业提交、过程记录、成果验收
>
> 对应大纲：Day01 云计算 + Linux 基础 / Day02 LNMP 部署 + WordPress 上线 / 调优与排错

------

## 文档控制页


| 文档编号 |             OPS-TRAIN-2026-001             | 实训项目 | 基于阿里云 ECS 的 WordPress 博客系统部署 |
| :------: | :----------------------------------------: | :------: | :--------------------------------------: |
| 文档版本 |                    V1.0                    | 实训时长 |                   3 天                   |
| 实训小组 |              【填写小组名称】              | 组员姓名 |             【填写全体组员】             |
| 指导教师 |              【填写老师姓名】              | 提交日期 |             【填写提交日期】             |
| 实训环境 | 阿里云 ECS + CentOS 7.9 + LNMP + WordPress | 成绩评定 |                 教师填写                 |

### 修订记录


| 版本 | 修订日期 |           修订内容           | 修订人 |
| :--: | :------: | :--------------------------: | :----: |
| V1.0 |          | 初始版本，完成全流程部署记录 |        |

------

## 一、项目概述

### 1.1 实训目的

【实训填写：简述本次实训的目标，例如掌握云服务器申请、Linux 基础操作、LNMP 环境搭建、CMS 系统部署的完整流程，理解 Web 服务运行原理】

### 1.2 实训背景与技术选型说明

1. **为什么选择阿里云 ECS**

   【实训填写：简述云服务器相比传统物理服务器的优势，以及选择阿里云的原因】

2. **为什么选择 LNMP 架构**

   【实训填写：简述 LNMP 各组件的作用，对比 LAMP 说明 Nginx 的优势】

### 1.3 实训最终成果

【实训填写：列出本次实训完成的产出，例如：1 台可运行的云服务器、1 个公网可访问的博客网站、1 份完整运维文档】

------

## 二、环境资源清单

### 2.1 云服务器硬件配置


|    配置项    |     实际参数     |              备注               |
| :----------: | :--------------: | :-----------------------------: |
|    云厂商    |      阿里云      |                                 |
|     地域     |                  |      例如：华南 1（广州）       |
|   实例规格   |                  | 例如：1 核 2G ecs.t6-c1m1.large |
|   操作系统   | CentOS 7.9 64 位 |            公共镜像             |
|    系统盘    |                  |       例如：40G 高效云盘        |
|   实例 ID    |                  |         控制台复制填写          |
| 公网 IP 地址 |                  |       核心信息，务必准确        |
|   付费模式   |                  |    例如：按量付费 / 免费试用    |

### 2.2 网络与安全组配置


| 规则方向 | 端口 | 协议 | 授权对象  | 用途说明 |
| :------: | :--: | :--: | :-------: | :------: |
|  入方向  |  22  | TCP  | 0.0.0.0/0 |          |
|  入方向  |  80  | TCP  | 0.0.0.0/0 |          |
|  入方向  | 443  | TCP  | 0.0.0.0/0 |          |
|  入方向  | 3306 | TCP  |           |          |

【实训填写：简述安全组的作用，以及为什么必须开放对应端口】

### 2.3 软件版本清单


| 组件名称  | 实际版本 |    安装方式    | 核心作用 |
| :-------: | :------: | :------------: | :------: |
|   Nginx   |          |      yum       |          |
|  MariaDB  |          |      yum       |          |
|    PHP    |          | yum（remi 源） |          |
|  PHP-FPM  |          | yum（remi 源） |          |
| WordPress |          |   官网源码包   |          |

------

## 三、部署前期准备（Day 01 实训内容）

### 3.1 阿里云 ECS 实例创建

**操作步骤记录：**

1. 账号注册与实名认证：【填写完成情况，遇到的问题】
2. 领取免费额度 / 学生认证：【填写领取的额度 / 套餐】
3. 创建实例关键配置选择：
   - 镜像选择：CentOS 7.9，选择原因：【填写】
   - 登录凭证：自定义密码，root 密码为：【自行记录，勿泄露】
4. 实例启动验证：控制台实例状态为「运行中」

### 3.2 远程连接服务器

1. **SSH 是什么**

   【实训填写：用自己的话简述 SSH 协议的作用和特点】

2. **连接方式与结果**

   本次使用的连接工具：【例如：Workbench / Xshell / 系统终端】

   连接命令（终端方式）：

```bash

```





连接成功截图粘贴处：

【此处粘贴登录成功的终端截图】

### 3.3 系统初始化配置

**执行命令与结果记录：**

1. 更新系统软件包

```bash
yum update -y
```

执行结果：【成功 / 失败，失败请说明原因】

1. 安装基础工具包

```bash
yum install -y vim wget net-tools lsof unzip
```

执行结果：

1. 关闭 SELinux

   临时关闭命令：

   永久关闭命令：

   验证命令 `getenforce` 输出结果：

2. 关闭系统防火墙

   停止命令：

   禁止开机自启命令：

   验证状态输出：

### 3.4 Linux 基础认知与命令实操

1. **Linux 核心认知**

   - 简述 Linux 内核与发行版的区别：【填写】
   - 简述 Linux “一切皆文件” 的设计哲学：【填写】

2. **目录结构验证**

   执行 `ls /` 命令，对照说明以下目录的作用：

   | 目录 | 作用说明 |

   |---|---|

   | /root |  |

   | /etc |  |

   | /usr |  |

   | /var |  |

   | /tmp |  |

   | /home |  |

3. **核心命令实操记录**

   【实训填写：执行以下命令，记录功能与执行结果，可附截图】

表格

|            命令            | 功能说明 | 执行结果 |
| :------------------------: | :------: | :------: |
|           `pwd`            |          |          |
|          `ls -l`           |          |          |
|      `cd /` + `cd ~`       |          |          |
|    `mkdir -p test/a/b`     |          |          |
|      `touch test.txt`      |          |          |
|    `cp test.txt test/`     |          |          |
|   `mv test.txt new.txt`    |          |          |
|      `cat /etc/hosts`      |          |          |
|  `tail -n 5 /etc/passwd`   |          |          |
| `grep "root" /etc/passwd`  |          |          |
| `find /etc -name "*.conf"` |          |          |

> 实训要求：所有命令必须手动敲入执行，禁止直接复制粘贴，熟练使用 Tab 补全技巧。

------

## 四、LNMP 环境部署（Day 02 核心内容）

### 4.1 LNMP 架构理论

1. **Web 服务工作原理简述**

   【实训填写：用自己的话描述用户输入 URL 到看到网页的完整流程】

2. **LNMP 架构说明**

   画出 LNMP 架构图，并标注各组件的分工：

   【此处粘贴架构图 / 手绘拍照】

### 4.2 第一步：安装 Nginx

**执行命令：**



```bash
# 安装
yum install -y nginx

# 启动+开机自启
systemctl start nginx
systemctl enable nginx

# 验证
systemctl status nginx
nginx -v
```

**验证结果：**

- Nginx 版本号：

- 服务状态：

- 浏览器访问公网 IP，截图：

  

  【粘贴 Nginx 欢迎页截图】

**Nginx 关键路径记录：**

- 主配置文件：
- 站点配置目录：
- 默认网站根目录：
- 日志目录：

### 4.3 第二步：安装 MariaDB 数据库

**执行命令：**

```bash
# 安装
yum install -y mariadb-server mariadb

# 启动+开机自启
systemctl start mariadb
systemctl enable mariadb

# 安全初始化
mysql_secure_installation
```

**初始化操作记录：**

- root 数据库密码设置为：
- 是否移除匿名用户：
- 是否禁止 root 远程登录：
- 是否删除 test 库：

**验证结果：**

执行 `mysql -u root -p` 可正常登录数据库：是 / 否

### 4.4 第三步：安装 PHP 与 PHP-FPM

**执行命令：**


```bash
# 安装epel和remi源
yum install -y epel-release
yum install -y https://rpms.remirepo.net/enterprise/remi-release-7.rpm

# 启用PHP 7.4
yum install -y yum-utils
yum-config-manager --enable remi-php74

# 安装PHP及扩展
yum install -y php php-fpm php-mysqlnd php-gd php-mbstring php-xml php-curl

# 修改运行用户为nginx
sed -i 's/user = apache/user = nginx/g' /etc/php-fpm.d/www.conf
sed -i 's/group = apache/group = nginx/g' /etc/php-fpm.d/www.conf

# 启动
systemctl start php-fpm
systemctl enable php-fpm
```

**验证结果：**

- PHP 版本号：
- PHP-FPM 服务状态：
- 为什么要把 PHP-FPM 用户改成 nginx：【填写】

### 4.5 第四步：配置 Nginx 支持 PHP 解析

**配置文件内容：**

```bash
/etc/nginx/conf.d/default.conf
```



```nginx

```

（将完整配置粘贴在此处）

**配置校验与重载命令：**


```bash
nginx -t
nginx -s reload
```

校验结果：

**PHP 解析验证：**

创建 info.php 测试文件，访问截图粘贴在此处：

【粘贴 phpinfo 页面截图】

> 验证完成后删除测试文件，命令：`rm -f /usr/share/nginx/html/info.php`

------

## 五、WordPress 博客系统部署

### 5.1 WordPress 简介

【实训填写：简述 WordPress 是什么，以及它的核心优势】

### 5.2 第一步：创建专属数据库与用户

**执行的 SQL 语句：**

```sql

```

（粘贴完整 SQL）

**验证：**

使用 wpuser 账号可正常登录并访问 wordpress 库：是 / 否

### 5.3 第二步：下载并部署 WordPress 源码

**执行命令：**


```bash
# 下载
cd /tmp
wget https://cn.wordpress.org/latest-zh_CN.tar.gz

# 解压
tar zxf latest-zh_CN.tar.gz

# 移动到网站根目录
rm -f /usr/share/nginx/html/index.html
mv wordpress/* /usr/share/nginx/html/
```

### 5.4 第三步：配置目录权限

**执行命令：**


```bash
chown -R nginx:nginx /usr/share/nginx/html/
```

**为什么必须修改权限？不修改会有什么问题？**

【实训填写】

### 5.5 第四步：Web 引导安装

1. 访问地址：`http://公网IP`
2. 数据库配置信息：
   - 数据库名：
   - 用户名：
   - 密码：
   - 数据库主机：
3. 站点管理员信息：
   - 站点标题：
   - 管理员账号：
   - 管理员密码：

**安装成功截图：**

【粘贴后台仪表盘截图】

### 5.6 第五步：发布第一篇博文

操作记录：

1. 进入后台 → 文章 → 写文章
2. 输入标题、正文、上传图片
3. 发布并前台验证

**前台博文截图：**

【粘贴文章页截图】

------

## 六、企业级调优配置（拓展加分项）

### 6.1 解锁 PHP 上传与内存限制

**修改的配置文件：**`/etc/php.ini`

**修改的参数与值：**

- upload_max_filesize =
- post_max_size =
- memory_limit =

**生效命令：**


```bash
systemctl restart php-fpm
```

验证结果：

### 6.2 免除 FTP 提示，开启直接热更新

**执行命令：**


```bash
echo "define('FS_METHOD', 'direct');" >> /usr/share/nginx/html/wp-config.php
```

**作用说明：**

【实训填写】

------

## 七、部署验收标准

逐项验证并打勾，全部通过即为部署完成。

### 7.1 服务状态验收

-  Nginx 服务运行中，开机自启
-  MariaDB 服务运行中，开机自启
-  PHP-FPM 服务运行中，开机自启
-  80 端口正常监听，公网可访问
-  22 端口正常，可远程 SSH 连接

### 7.2 业务功能验收

-  公网 IP 可正常打开博客首页
-  PHP 页面解析正常，不显示源码
-  WordPress 后台可正常登录
-  可正常发布、编辑文章
-  可正常上传图片 / 媒体文件
-  设置固定链接后文章页无 404
-  数据库连接正常，无报错

------

## 八、故障排查记录（实训核心，答辩加分项）

> 要求：至少记录 2 个实操中遇到的真实问题，禁止写 “无任何问题”。

### 问题 1

- **故障现象**：
- **原因分析**：
- **解决过程**：
- **最终结果**：

### 问题 2

- **故障现象**：
- **原因分析**：
- **解决过程**：
- **最终结果**：

### 问题 3（可选）

- **故障现象**：
- **原因分析**：
- **解决过程**：
- **最终结果**：

------

## 九、实训总结与心得

1. **知识收获**：

   【填写本次实训学到的核心知识点】

2. **能力提升**：

   【填写在实操能力、排错能力、团队协作等方面的提升】

3. **不足与改进方向**：

   【填写本次实训中做得不好的地方，以及后续可以优化学习的方向】

4. **对课程的建议**：

   【可选填写】

------

## 十、附录

### 10.1 关键文件路径速查表


|          组件          | 路径 | 说明 |
| :--------------------: | :--: | :--: |
|      Nginx 主配置      |      |      |
|   Nginx 站点配置目录   |      |      |
|       网站根目录       |      |      |
|     Nginx 错误日志     |      |      |
|      MariaDB 配置      |      |      |
|       PHP 主配置       |      |      |
|   WordPress 核心配置   |      |      |
| WordPress 主题插件目录 |      |      |

### 10.2 常用运维命令

【自行整理本次实训中最常用的 10 条命令】